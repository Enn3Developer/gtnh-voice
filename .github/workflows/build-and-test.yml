
name: Build and test

on:
  pull_request:
    branches: [ master ]
  push:
    branches: [ master ]
  workflow_call:

jobs:
  build-and-test:
    uses: GTNewHorizons/GTNH-Actions-Workflows/.github/workflows/build-and-test.yml@master
    secrets: inherit
